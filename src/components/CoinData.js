import React, { useEffect } from 'react';

const CoinData = ({ data }) => {

    useEffect(() => {
        if (data) {
            
        }
        
    },[data]);

    return (
        <div>Data</div>
    )
}

export default CoinData;
